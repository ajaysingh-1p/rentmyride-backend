spring.application.name=rentmyride-backend
server.port=${SERVER_PORT:8080}
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true
 
jwt.expiration=86400000

# ── Razorpay ────────────────────────────────────────
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_TIDF2iGRgQtXXZ}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:egZfbdYv0tHSMofNZhRS6WCk}
razorpay.currency=INR

razorpay.webhook.secret=${RAZORPAY_WEBHOOK_SECRET:}

# ── GST ─────────────────────────────────────────────
gst.percentage=18.0
gst.cgst=9.0
gst.sgst=9.0


spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

file.upload-dir=${FILE_UPLOAD_DIR:${user.home}/rentmyride-uploads}

self-drive.deposit-refund-method=${SELF_DRIVE_REFUND_METHOD:WALLET}

cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}


spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:ajaykr11012003@gmail.com}
spring.mail.password=${SMTP_PASSWORD:jzrlnyyzwekdstfh}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ── OTP ───────────────────────────────────────────────
otp.expiry-minutes=5
otp.length=6
otp.max-attempts=5
otp.resend-cooldown-seconds=30
otp.rate-limit.max-requests=20
otp.rate-limit.window-minutes=15

notification.enabled=${NOTIFICATIONS_ENABLED:false}
twilio.account-sid=${TWILIO_ACCOUNT_SID:YOUR_TWILIO_ACCOUNT_SID}
twilio.auth-token=${TWILIO_AUTH_TOKEN:YOUR_TWILIO_AUTH_TOKEN}
twilio.sms-from=${TWILIO_SMS_FROM:+1XXXXXXXXXX}
twilio.whatsapp-from=${TWILIO_WHATSAPP_FROM:whatsapp:+14155238886}
# Admin's WhatsApp number that unanswered customer chatbot queries get escalated to
twilio.admin-whatsapp-number=${TWILIO_ADMIN_WHATSAPP:whatsapp:+91XXXXXXXXXX}

springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
